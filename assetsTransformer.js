// const path = require('path');

// module.exports = {
//   process(src, filename) {
//     return `module.exports = ${JSON.stringify(path.basename(filename))};`;
//   },
// };

// This file is here for the PNG that is imported into WeGotDividerLine.
// Without this, Jest errors out when loading the .png file.
// This is the reason why the Jest configuration in the package.json exists.
